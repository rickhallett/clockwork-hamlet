# Start Ticket

Mark a ticket as in_progress and begin work. Argument: $ARGUMENTS

1. Run: `ticket start $ARGUMENTS`
2. Show ticket details: `ticket show $ARGUMENTS`
3. Summarize what needs to be done based on ticket title/description
