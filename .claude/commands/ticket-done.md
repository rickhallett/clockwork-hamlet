# Complete Ticket

Mark a ticket as done. Argument: $ARGUMENTS

1. Run: `ticket done $ARGUMENTS`
2. Confirm completion
3. Remind: commit should reference ticket key `[$ARGUMENTS]` if not already done
